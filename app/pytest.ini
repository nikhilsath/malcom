[pytest]
pythonpath = .
testpaths = app/tests
cache_dir = app/tests/.pytest_cache
markers =
    smoke: route-level smoke coverage used by the full test gate
    integration: tests that exercise integrated backend workflows against PostgreSQL
