# TASK-003 Affected Tests Report

Found tests referencing the builder connector endpoint or automation builder flows:

- `tests/test_automations_api.py` — calls `/api/v1/automations/workflow-connectors` (integration test). Update expected payload shape if endpoint changes; consider adding tests for normalized fields and filtering by compatibility/status.
- `tests/api_smoke_registry/automation_log_cases.py` — registers smoke test for `GET /api/v1/automations/workflow-connectors`. Update registry entry if endpoint path changes or schema changes.
- `tests/test_http_presets.py` — exercises HTTP presets in workflow builder; may indirectly cover builder flows. Review for any assumptions about connector list or presets merging.

Potential frontend/unit tests to check (search UI for component tests referencing connector):
- UI component tests under `ui/src/automation/**` that reference `ConnectorActivityStepForm` or `workflow-connectors` network mocks. (Search required in `ui/`.)

Playwright/e2e tests to review:
- `ui/e2e/` tests that open the automation builder modal or add-step flows. Search Playwright tests for selectors `add-step-connector-input` or network stubs for `/api/v1/automations/workflow-connectors`.

Recommendations / next steps:
1. Update backend tests: extend `tests/test_automations_api.py` to assert normalized fields and filtering behavior for connector compatibility and status.
2. Update `tests/api_smoke_registry/` entry if endpoint path or response shape changes.
3. Search `ui/` for component tests and Playwright tests and update mocks to point to the verified endpoint. Create a small checklist mapping each UI test file to required mock updates.

Concrete test-change checklist:

- `tests/test_automations_api.py`: add assertions that the response for `/api/v1/automations/workflow-connectors` contains at minimum: `id`, `name`, `provider`, `status`, `auth_type`, `scopes` (array), `owner`. Add a test case ensuring connectors with `status` in `INACTIVE_WORKFLOW_CONNECTOR_STATUSES` are not returned.
- `tests/api_smoke_registry/automation_log_cases.py`: keep the smoke entry but update expected response shape validator if schema changes.
- Add `tests/test_connectors_for_builder.py` (new): unit-style tests covering empty list, single active connector, incompatible connector filtered, and DB error handling (mock DB to throw).

Frontend test mapping (search `ui/src/automation/**`):
- `ui/src/automation/__tests__/ConnectorActivityStepForm.test.tsx` (if present): update network mock to `/api/v1/automations/workflow-connectors` and assert rendering of `add-step-connector-input`, loading state, empty state, and selection behavior.
- Playwright: locate e2e test(s) that open the add-step modal (e.g., `ui/e2e/automation/*.spec.ts`) and update route stubs/mocks for the normalized connector payloads (loading/empty/error/success scenarios).

Ordering recommendation:
1. Update backend tests and implement any normalization changes.
2. Update smoke registry and backend CI checks.
3. Update frontend unit tests and component code.
4. Update Playwright tests to run against updated mocks.

Record of files to create/update will be assembled as implementation begins.


Generated by TASK-003 automated impact discovery.
