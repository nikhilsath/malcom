// Documentation Browse page controller

const browseList = document.getElementById("docs-browse-list");
const emptyMessage = document.getElementById("docs-browse-empty");

if (browseList && emptyMessage) {
  emptyMessage.hidden = false;
}
